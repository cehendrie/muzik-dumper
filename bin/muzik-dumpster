#!/usr/bin/env python

import sys
import muzikdumpster.core

if __name__ == '__main__':
    try:
        result = muzikdumpster.core.main()
    except Exception as e:
        print('[Error] process failed to complete - exiting...')
        print(e.message)
        sys.exit(1)

    sys.exit(result)
